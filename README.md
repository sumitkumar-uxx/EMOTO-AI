# EMOTO-AI
EMOTO\AI is a machine learning project that predicts human emotions from text. It uses TF-IDF for text vectorization and LightGBM for classification. The model is trained on a tweet emotion dataset and deployed with a Streamlit web app where users can enter their thoughts and get predicted emotions.
